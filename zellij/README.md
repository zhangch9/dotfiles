# Configurations for [Zellij](https://github.com/zellij-org/zellij)

## Usage

```bash
# (Optional) backup the existing configuration directory
mv ${XDG_CONFIG_HOME}/zellij ${XDG_CONFIG_HOME}/zellij.bak
ln -s $(pwd -P) ${XDG_CONFIG_HOME}
```
