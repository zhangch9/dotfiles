# Configuration files for [mpv](https://mpv.io)

## Usage

```bash
# backup old configuration files if exists
mv ${XDG_CONFIG_HOME}/mpv ${XDG_CONFIG_HOME}/mpv.bak
ln -s mpv ${XDG_CONFIG_HOME}/mpv
```
